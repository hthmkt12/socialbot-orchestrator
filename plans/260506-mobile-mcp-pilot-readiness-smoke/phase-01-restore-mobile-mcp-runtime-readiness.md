---
title: "Phase 01 Restore Mobile MCP Runtime Readiness"
status: blocked
priority: P0
created: 2026-05-06
---

# Phase 01: Restore Mobile MCP Runtime Readiness

## Context Links

- Runtime scripts: `package.json:18-27`, `package.json:35-39`.
- Mobile MCP guidance: `README.md:183-220`.
- Common issue runbook: `docs/common-issues.md:89-95`.
- Pilot backend source of truth: `docs/backend-capability-matrix.md:7-14`, `docs/backend-capability-matrix.md:63-73`.
- Current blocker evidence: latest `npm.cmd run status:mobile-mcp` showed ADB online none, bridge/worker/UI down, expected serial `QC4DKJUO6PW4FMQW` missing.

## Overview

Priority: P0. Restore local runtime truth before any UI smoke. This phase is operational verification, not code implementation.

## Requirements

- Detect whether expected Android serial is correct and online.
- Start or verify Mobile MCP runtime services.
- Run readiness checks in low-cost to high-cost order.
- Stop if secrets are missing; do not read/print `.env`.
- Store command outputs/report paths for Phase 03.

## Related Files

- Read only: `package.json`.
- Read only: `docs/common-issues.md`.
- Evidence output: `plans/reports/*.json` generated by existing scripts.
- No source code changes expected.

## Implementation Steps

- [x] **Step 1: Confirm git state before runtime work**

  Run:

  ```powershell
  git status --short --branch
  ```

  Expected: current branch visible; no unexpected source changes. If generated evidence files appear later, record them.

- [x] **Step 2: Check current Mobile MCP status**

  Run:

  ```powershell
  npm.cmd run status:mobile-mcp
  ```

  Expected: either all services/device ready, or actionable warnings. Save report path printed by the command.

- [x] **Step 3: If expected serial is missing, diagnose device visibility**

  Run:

  ```powershell
  npm.cmd run diagnose:mobile-mcp:devices
  ```

  Expected success: expected serial visible. Expected failure: report lists missing serial and recommendations. If phone is physically unavailable, stop and mark phase blocked; do not fake readiness.

- [x] **Step 4: Recover ADB only if diagnosis points to transport failure**

  Run only when needed:

  ```powershell
  npm.cmd run recover:mobile-mcp:adb
  ```

  Expected: ADB recovers or prints failure. Re-run Step 3 after recovery.

- [x] **Step 5: Start local runtime if bridge/worker/UI are down**

  Run in a dedicated terminal and keep it open:

  ```powershell
  npm.cmd run runtime:mobile-mcp
  ```

  Expected: Mobile MCP bridge, worker, Vite UI, and startup device sync remain running. Do not proceed until status can reach ready or produces a clear blocker.

  Result (2026-06-27): started bridge, worker, and Vite UI. All three healthy. Device `97249fb5` (Redmi/onyx, Android 16) connected via ADB. Fixed `spawn EINVAL` bug (missing `shell: isWindows`) in `start-mobile-mcp-local-runtime.mjs`.

- [x] **Step 6: Run preflight after runtime starts**

  Run:

  ```powershell
  npm.cmd run preflight:mobile-mcp
  ```

  Expected: exit code 0 and generated report with `ok: true`. If it fails, use the report's failed checks; do not proceed to full verify.

  Result (2026-06-27): 12/12 local checks pass (ADB, bridge, worker, UI). Supabase-dependent checks (auth, DB) fail with `ENOTFOUND gzwwqhgvrfsqokrxfhyu.supabase.co` — DNS resolution failure on this machine. Fixed bridge serial field mismatch (`device.id` → `device.id ?? device.serial`) in preflight and wait-devices scripts.

- [x] **Step 7: Run full verify only after preflight passes**

  Run:

  ```powershell
  npm.cmd run verify:mobile-mcp
  ```

  Expected: exit code 0 and a current `mobile-mcp-verify-*.json` report with `ok: true`.

  Result (2026-06-27): Steps `runtime.check` and `devices.wait` passed. Blocked at `operator.ensure` — Supabase DNS unresolvable. Full report at `plans/reports/mobile-mcp-verify-2026-06-27T01-40-03-819Z.json`.

- [x] **Step 8: Record readiness evidence paths**

  Capture these values for Phase 03:

  ```text
  status report: plans/reports/mobile-mcp-status-2026-06-27T01-31-37-864Z.json
  preflight report: plans/reports/mobile-mcp-preflight-2026-06-27T01-39-50-568Z.json
  verify report: plans/reports/mobile-mcp-verify-2026-06-27T01-40-03-819Z.json
  device serial: 97249fb5 (Redmi/onyx, was QC4DKJUO6PW4FMQW)
  ```

## Success Criteria

- `npm.cmd run preflight:mobile-mcp` passes from current shell.
- `npm.cmd run verify:mobile-mcp` passes from current shell.
- If blocked, blocker is external/operational and documented with report path.

## Execution Result

- Completed Step 1: `git status --short --branch` showed `master` with only the new plan folder untracked.
- Completed Step 2: `npm.cmd run status:mobile-mcp` generated `plans/reports/mobile-mcp-status-2026-05-06T10-58-12-223Z.json`.
- Completed Step 3: `npm.cmd run diagnose:mobile-mcp:devices` generated `plans/reports/mobile-mcp-wait-devices-2026-05-06T10-58-27-212Z.json`.
- ADB recovery ran through the diagnosis command and restarted the ADB daemon.
- Blocker: ADB still reports no devices, bridge is down, and doctor diagnosis says Windows does not see an Android USB device.
- Recommended physical fix: replug cable, unlock phone, select USB data/debug mode, confirm USB debugging authorization, check driver/port, then rerun this phase.

## Risks

- Expected device not physically connected.
- Bridge dependencies not installed.
- Supabase service credential unavailable in shell.
- UI smoke credential missing.

## Unresolved Questions

- Is the expected serial `QC4DKJUO6PW4FMQW` still valid?
