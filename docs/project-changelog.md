# Project Changelog

## 2026-06-27

- **Social pivot Phase 0 (Foundation) started**: Repositioned platform for social media automation (Instagram, TikTok, Facebook). Updated README, project-overview-pdr, roadmap, and codebase-summary with social-first positioning and 6-phase strategic roadmap.
- **Account schema**: Created migration `20260627000001_account_tables.sql` with `accounts` table (platform, warm-up stages, action limits, block detection) and `account_action_history` table (per-action audit trail). RLS enforced, indexes added.
- **Account types and hooks**: Added `Account`, `AccountActionHistory` types to `database.types.ts`. Created `account-service-helpers.ts` with full CRUD + action recording. Created `use-accounts.ts` React Query hooks (useAccounts, useAccount, useCreateAccount, useUpdateAccount, useDeleteAccount, useAccountHistory, useRecordAccountAction).
- **Account Setup UI**: New `/account-setup` page with accounts table (username, platform badge, warm-up stage, action counts, blocked status), create modal (username, password, platform, daily limit), and delete. Lazy-loaded at 7.5 kB. Added "Accounts" nav link in sidebar.
- **Anti-detection engine**: Created `anti-detection-helpers.ts` with randomized delays, scroll variance, tap coordinate jitter, and cooldown utilities. Defined `AntiDetectionConfig` interface in `contracts/macro.ts` as canonical source. Sidebar branding updated to "Social Automation".
- **Social engagement templates**: Pre-built `MacroDefinition` workflows for Instagram (like hashtag, follow accounts) and TikTok (like trending) in `social-engagement-templates.ts`. Registered in macro starter template picker alongside generic templates.
- **Anti-detection execution integration**: `SingleDeviceStepRunner` now applies coordinate jitter and delay randomization to resolved step params when `antiDetection` config present in `MacroDefinition`. Adds random cooldown between successful steps for human-like pacing.
- **Account warm-up progression engine**: Per-platform stage progression (Instagram/TikTok/Facebook) with day-based thresholds and recommended daily action limits. Account table shows computed stage info, Start Warm-Up button, advancement hints, and action limit status.
- **Account CSV import**: CSV parser with validation (platform check, column count, empty fields, daily_limit parsing). Import modal with file picker, preview table, error display, and sample template download. 9-unit-test coverage.
- **Account-to-macro mapping in run wizard**: New "Select Account" step in the run wizard that appears when a social engagement macro (with antiDetection config) is selected. Users pick from active Instagram/TikTok/Facebook accounts. Account info (username, platform) is injected into run input variables for downstream action tracking.
- **Social Dashboard page**: New `/social-dashboard` route with stat cards (total accounts, active, actions today, full speed), blocked-account alert, and per-account health cards showing warm-up stage, usage bar, action counts, and quick Start Warm-Up action.
- **Warm-up auto-advancement system**: `account-warmup-auto-advance.ts` detects accounts eligible for stage advancement (day thresholds met, active in warm-up stages 2-4). React hook (`use-warmup-auto-advance.ts`) and panel component (`warmup-progression-panel.tsx`) with per-account Advance buttons and batch "Advance All" in the Social Dashboard. `account-warmup-scheduler.ts` provides daily-check foundation with localStorage cooldown tracking.
- **Account action history modal**: Click any account health card in the Social Dashboard to open an action history timeline showing recent actions (like/follow/comment/post/share) with timestamps, success/failure indicators, and error details. Wired via existing `useAccountHistory` hook.
- **Multi-app macro step routing**: New `TargetApp` type and optional `targetApp` field on `MacroStep` for routing individual steps to Instagram/TikTok/Facebook. Target App selector added to each step card in the guided builder. Validation rejects invalid targetApp values.
- **Daily action count reset**: `account-action-reset.ts` resets `current_action_count` to 0 when a new calendar day starts and adjusts `daily_action_limit` per warm-up stage. Batch `runDailyActionReset()` integrated into the scheduler pipeline.

## 2026-06-26

- **Phase 1 (Mobilerun driver swap)**: Replaced `mobile-mcp-ai` Python package with Mobilerun `AndroidDriver` in `services/mobile-mcp-bridge/`. Updated imports for v0.6.8 API (`from mobilerun import AndroidDriver`, `from mobilerun_core_cli.portal import ensure_portal_ready`). Fixed `.ckignore` venv block with `!.venv` exception.
- **Phase 2 (Mobilerun backend + ai_task)**: Added `DEVICE_BACKEND=mobilerun` to worker config. Created `services/execution-worker/src/mobilerun-step-backend.ts` extending `MobileMcpStepBackend` with `ai_task` step via MobileAgent. Added `'ai_task'` to `StepType` union in contracts. Registered `_handle_ai_task` in bridge dispatch table. Changed `baseUrl` from `private` to `protected` for subclass access.
- **Phase 3 (iOS support)**: Refactored `android_session_manager.py` → platform-aware `DeviceSession` supporting both `AndroidDriver` (Android) and `IOSDriver` (iOS). Added iOS device discovery via `discover_ios_portal()` (port range 6643-6653). Platform-specific step guards: `get_current_app` and `adb` return errors on iOS; `press_button` validates against `driver.supported_buttons` (Android: back/home/enter, iOS: home only). Updated `bridge_server.py` to pass `platform` from device payload to session manager. Updated both TypeScript backends to extract `platform` from `device.metadata_json?.platform` (default `android`). Session keys changed to `(platform, identifier)` tuples. Backward-compat `AndroidSessionManager` alias preserved.

## 2026-05-06

- Created `plans/260506-mobile-mcp-pilot-readiness-smoke/` for the Mobile MCP pilot readiness and artifact-bearing Run Detail smoke slice.
- Rechecked current Mobile MCP readiness: `status:mobile-mcp` and `diagnose:mobile-mcp:devices` produced fresh reports, but the slice is blocked because Windows/ADB does not see expected serial `QC4DKJUO6PW4FMQW`.
- Kept pilot architecture decisions unchanged while blocked: Mobile MCP remains pilot default, Laixi remains future-compatible, inline artifacts remain the pilot artifact path, and multi-target execution remains sequential.
- Created Spec Kit feature `002-laixi-gateway-live-proof` and recorded current Laixi outcome: gateway health is OK locally, sessions are empty, and clean-path proof is blocked until Laixi VIP/API access and a live device session are available.
- Updated backend/pilot docs to keep Mobile MCP as the pilot-default backend while Laixi remains future-compatible rather than pilot-blocking.
- Created Spec Kit feature `003-artifact-storage-thresholds` and documented numeric thresholds for inline artifacts: 512,000-byte preview payloads, 10 artifacts per run, 5 screenshots per run, 30-day retention, and authenticated-app-only viewing.
- Completed and merged Spec Kit feature `003-artifact-storage-thresholds` into `master`.
- Completed Spec Kit feature `001-normalize-pilot-artifact`: run artifacts now expose friendly screenshot/log/JSON/unknown labels, linkage status, inline preview availability, and pilot storage status through the run evidence UI.
- Added focused coverage for artifact normalization in `src/lib/run-artifacts.test.ts` and verified with `npm.cmd test`, `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`.
- Kept artifact storage policy unchanged: inline `artifacts` row previews remain the pilot default, while object storage stays deferred until higher screenshot volume, longer retention, or external sharing.
- Deferred manual run-detail smoke until a running UI session with Supabase auth and an artifact-bearing completed run is available.

## 2026-05-05

- Initialized local ClaudeKit Engineer baseline with `ck.cmd init --yes`.
- Added repo-local `AGENTS.md` as canonical instruction entrypoint required by `CLAUDE.md`.
- Added `docs/common-issues.md` for recurring Windows, CK, secrets, and Mobile MCP runtime issues.
- Added this changelog so future feature, bug, security, and architecture changes have a durable log.
- Excluded `.claude/` from ESLint scope so repo lint checks app code instead of local CK skill sources.
- Added backend capability matrix and pilot decisions for artifact storage and sequential multi-target execution.
- Lazy-loaded authenticated page routes to reduce the main Vite chunk below the 500 kB warning threshold.
- Added file-size refactor plan for the largest source files.
- Extracted shared Device Setup UI cards and formatters into focused modules without changing runtime probe logic.
- Extracted Device Setup static tab bodies into a focused component module, keeping verification/probe logic in the page.
- Extracted Device Setup hero summary and tab navigation into a focused shell component.
- Extracted Device Setup Verify endpoint/checklist and live probe panels into focused components.
- Extracted Device Setup diagnostics, recovery actions, and selected-device summary into focused components.
- Extracted Device Setup verification checklist construction into a pure helper.
- Extracted Device Setup verification state and probe orchestration into a dedicated hook, leaving the page as a layout shell.
- Split Device Setup verification runtime services and derived UI state out of the hook.
- Extracted Device Setup probe result rendering into a focused card component.
- Extracted Device Setup selected-device summary and troubleshooting tab so all Device Setup files are below 200 lines.
- Refactored Run Wizard into focused step, shell, navigation, submit, and derived-state modules, reducing `RunWizard.tsx` below 300 lines.
- Extracted run artifact preview card rendering from `RunArtifactsPanel`.
- Started Demo page refactor by moving workflow constants, status mapping, icon mapping, and duration helper into a focused module.
- Refactored Demo page into focused configuration, execution, macro definition, and runtime helper modules, reducing `DemoPage.tsx` below 300 lines.
- Refactored Run Monitor into focused data, header, approval, summary, device list, status, and runtime helper modules, reducing `RunMonitorPage.tsx` below 200 lines.
- Refactored Devices page into focused summary, filter, risk panel, grid/card, drawer, icon, and type modules, reducing `DevicesPage.tsx` below 200 lines.
- Refactored macro guided builder into focused intro, basics, inputs, steps, parameter editor, and helper modules, reducing `macro-definition-builder-panel.tsx` below 50 lines.
- Refactored Approvals page into focused audit-focus banner, tabs, list/card, details modal, and shared type modules, reducing `ApprovalsPage.tsx` below 200 lines.
- Refactored Run Detail page into focused header, focus banner, progress, info cards, steps timeline, summary, status config, and shared type modules, reducing `RunDetailPage.tsx` below 150 lines.
- Refactored Macro Detail page into focused header, about, execution config, input schema, step tree, versions list, JSON modal, and shared config modules, reducing `MacroDetailPage.tsx` below 150 lines.
- Refactored Macros list page into focused header actions, search, grid/card, and seed-samples modal modules, reducing `MacrosPage.tsx` close to 100 lines.
- Refactored Audit Logs page into focused stats, domain chips, filters, list, and shared type modules, reducing `AuditLogsPage.tsx` below 150 lines.
- Refactored Device Groups page into focused grid, create modal, and member drawer modules, reducing `DeviceGroupsPage.tsx` below 100 lines.
- Refactored Runs page into focused summary, filters, list, status config, and shared type modules, reducing `RunsPage.tsx` below 100 lines.
- Continued Demo page refactor by moving device loading, live state polling, and live run creation into focused modules, reducing `DemoPage.tsx` below 200 lines.
- Extracted Run Wizard form state and target-selection helpers into a focused hook, reducing `RunWizard.tsx` below 200 lines.
- Split audit log insight helpers and summary generation into focused modules, reducing `audit-log-insights.ts` below 200 lines.
- Extracted Device Setup lock snapshot summarization into a focused module, reducing `device-setup-diagnostics.ts` below 200 lines.
- Split Laixi response mapper helpers into a focused adapter module, reducing `adapters/laixi/mapper.ts` below 150 lines.
- Split sample macro fixture data into `contracts/sample-macros.ts`, reducing `contracts/macro.ts` below 150 lines while preserving the existing `SAMPLE_MACROS` export.
- Extracted high-level Laixi client actions into a focused adapter helper, reducing `adapters/laixi/client.ts` below 200 lines.
- Added Vitest app test runner scoped to `src` and focused `run-preflight` tests before splitting preflight target validation, device warnings, helper logic, and step analysis into smaller modules.
- Added Device Setup helper tests and split `device-setup.ts` into focused URL, health fetch, probe, AutoJS script, and protocol example modules while keeping the existing facade exports.
- Added focused `WorkflowRunner` coverage for lock failures, retry control flow, approval rejection, conditional branching, and continue-on-error execution before splitting runner flow, device-step execution, and persistence into dedicated engine modules.
- Added focused `RunOrchestrator` coverage for single-device status mapping, mid-run cancellation, mixed multi-device summary updates, and `cancelRun` cleanup before splitting orchestration context builders, approval/screenshot handlers, and result helpers into dedicated engine modules.
- Extracted shared orchestration test fixtures and workflow-run Supabase mock helpers so `orchestrator.test.ts` stays below 200 lines while preserving the same public API coverage.
- Split run-query fetchers/refetch policies and run-control fallback requests out of `useRuns.ts` while keeping the same public hook/request exports for existing callers.
- Split `ApprovalDialog.tsx` into focused approval action logic and action-panel modules while preserving the same approval modal behavior and caller contract.
- Extracted `RunWizard` data-loading and derived-state wiring into a focused hook so the wizard shell stays thin while preserving the same step components and submit flow.
- Extracted Run Wizard derived-state helpers for macro filtering, input-field derivation, target-device resolution, dispatchable counts, and fleet summaries into `run-wizard-derived-helpers.ts`, keeping `use-run-wizard-derived-state.ts` below 170 lines without changing wizard behavior.
- Extracted Demo configuration live-device selection and mode/role notice rendering into `demo-configuration-sections.tsx`, reducing `DemoConfigurationPanel.tsx` from 192 to 130 lines without changing demo run behavior.
- Extracted runner conditional and approval control-flow handling into `runner-control-flow.ts`, reducing `runner-step-flow.ts` from 202 to 132 lines while preserving the existing `WorkflowRunner` execution contract.
- Extracted approval status/config helpers and requested-action parsing into `approval-insight-helpers.ts`, reducing `approval-insights.ts` from 190 to 50 lines while preserving the existing `buildApprovalInsight()` API.
- Extracted Device Setup probe/reconnect/expired-lock action handlers into `use-device-setup-verification-actions.ts`, reducing `use-device-setup-verification.ts` from 197 to 143 lines while preserving the existing hook contract for `DeviceSetupPage`.
- Extracted macro authoring mode toggle, JSON editor, validation error, and footer action sections into `macro-definition-authoring-sections.tsx`, reducing `macro-definition-authoring-modal.tsx` from 190 to 166 lines without changing authoring flow behavior.
- Extracted guided macro step-card editing controls into `macro-builder-step-card.tsx`, reducing `MacroBuilderStepsSection.tsx` from 189 to 95 lines while preserving the existing builder step update contract.
- Split macro query fetchers and mutation request bodies into `macro-query-helpers.ts` and `macro-mutation-requests.ts`, reducing `useMacros.ts` from 183 to 69 lines while preserving the existing macro hook exports for callers.
- Extracted Run Wizard step-content switching and navigation state into `run-wizard-step-content.tsx` and `use-run-wizard-navigation-state.ts`, reducing `RunWizard.tsx` from 184 to 163 lines while preserving the existing wizard flow and caller contract.
- Extracted Device Setup operator diagnostics and recovery panels into `device-setup-operator-diagnostics-panel.tsx` and `device-setup-recovery-actions-panel.tsx`, leaving `device-setup-diagnostics-panels.tsx` as a re-export surface without changing `DeviceSetupPage` imports.
- Extracted Run Evidence stats, step-group rendering, and unlinked artifact rendering into `run-artifacts-sections.tsx`, reducing `RunArtifactsPanel.tsx` from 177 to 83 lines while preserving the existing `RunDetailPage` panel contract.
- Extracted audit log row action/resource config and expanded detail rendering into `audit-log-row-config.ts` and `audit-log-row-details.tsx`, reducing `AuditLogRow.tsx` from 166 to 77 lines while preserving the existing `AuditLogsList` row props and expand/collapse behavior.
- Extracted run wizard review summary card and preflight panel rendering into `run-wizard-review-sections.tsx`, reducing `RunWizardReviewStep.tsx` from 167 to 54 lines while preserving the existing review-step props and wizard dispatch readiness behavior.
- Extracted approval card, actions, and status styling into `approvals-list-sections.tsx`, reducing `ApprovalsList.tsx` from 164 to 69 lines while preserving the existing list props and approval action behavior.
- Extracted run monitor device and step cards into `run-monitor-device-sections.tsx`, reducing `RunMonitorDeviceList.tsx` from 164 to 25 lines while preserving the existing monitor list props and expand/collapse toggle behavior.
- Extracted approval details content, facts, metadata, and reject form into `approval-details-sections.tsx`, reducing `ApprovalDetailsModal.tsx` from 162 to 51 lines while preserving the existing modal props and approval resolution behavior.
- Extracted run wizard target mode grid, contract notice, device picker, group picker, and all-devices summary into `run-wizard-target-sections.tsx`, reducing `RunWizardTargetStep.tsx` from 161 to 73 lines while preserving the existing wizard target props and selection behavior.
- Extracted guided builder step catalog into `macro-builder-config.ts` and clone/ID/compatibility helpers into `macro-builder-helpers.ts`, reducing `macro-builder.ts` from 160 to 72 lines while preserving the existing guided builder exports and behavior.
- Extracted social DSL slugging, target-param mapping, and step compilation helpers into `social-macro-dsl-helpers.ts`, reducing `social-macro-dsl.ts` from 160 to 108 lines while preserving the existing social DSL exports and compile behavior.
- Extracted wizard target mode option rendering into `run-wizard-target-mode-card.tsx`, reducing `run-wizard-target-sections.tsx` from 194 to 177 lines while preserving the existing wizard target props and selection behavior.
- Extracted approval fetch/update/requeue/cancel helpers into `approval-service-helpers.ts`, reducing `approval-service.ts` from 159 to 115 lines while preserving the existing approval service exports and decision behavior.
- Extracted run wizard review metrics and preflight issue-card primitives into `run-wizard-review-primitives.tsx`, reducing `run-wizard-review-sections.tsx` from 174 to 141 lines while preserving the existing review-step summary and preflight panel behavior.
- Extracted run wizard target contract and all-devices fleet notices into `run-wizard-target-notices.tsx`, reducing `run-wizard-target-sections.tsx` from 187 to 132 lines while preserving the existing target-mode selection and fleet summary behavior.
- Extracted shared run inputs, aggregate summary, and execution-result factories into `orchestrator-test-helpers.ts`, reducing `orchestrator.test.ts` from 217 to 186 lines while preserving the same cancellation, multi-device summary, and workflow-status assertions.
- Extracted Device Setup diagnostic types and grouped issue builders into `device-setup-diagnostic-types.ts` and `device-setup-diagnostic-builders.ts`, reducing `device-setup-diagnostics.ts` from 192 to 39 lines while preserving `buildDeviceSetupDiagnostics()` as the stable public entry point.
- Extracted runner device fixture, execution-context factory, and harness spy setup into `runner-test-helpers.ts`, reducing `runner.test.ts` from 191 to 138 lines while preserving the same lock, retry, approval, and conditional-flow assertions.
- Extracted Laixi transport defaults, listener/request types, socket-ready check, request-id generation, and response mapping into `client-transport-helpers.ts`, reducing `client.ts` from 186 to 185 lines while preserving the existing `LaixiClient` API and reconnect/request behavior.
- Extracted runner step retry timing, exception classification, and failed-step finalization into `runner-device-step-helpers.ts`, reducing `runner-device-step.ts` from 183 to 158 lines while preserving the existing device-step execution contract and failure artifact behavior.
- Extracted service health, device availability, and probe issue cohorts into `device-setup-diagnostic-cohorts.ts`, reducing `device-setup-diagnostic-builders.ts` from 190 to 77 lines while preserving `buildDeviceSetupDiagnostics()` and the existing diagnostic severity/order behavior.
- Extracted orchestration test reset helpers, secondary-device fixture, partial-run summaries, and workflow status assertion helpers into `orchestrator-test-helpers.ts`, reducing `orchestrator.test.ts` from 186 to 171 lines while making the shared test-helper surface the new largest remaining test file.
- Split orchestration test support helpers out of `orchestrator-test-helpers.ts` into `orchestrator-test-support.ts`, reducing the helper surface from 191 to 141 lines while preserving the same reset and workflow-status assertion behavior.
- Extracted device live-probe selector and action buttons into `device-setup-live-probe-controls.tsx`, reducing `device-setup-verify-panels.tsx` from 175 to 152 lines while preserving the same selected-device and probe-trigger behavior.
- Expanded `client-transport-helpers.ts` with websocket lifecycle binding, JSON message parsing, pending-request resolution, and shared failure responses, reducing `client.ts` from 185 to 170 lines while preserving the existing `LaixiClient` API and reconnect/request behavior.
- Extracted Device Setup verify-tab composition into `device-setup-verify-tab.tsx`, reducing `DeviceSetupPage.tsx` from 172 to 132 lines while preserving the existing verification hook wiring, selected-device state, and recovery/probe behavior.
- Moved Run Wizard macro/version selection, declared target derivation, and preflight argument assembly into `run-wizard-derived-helpers.ts`, reducing `use-run-wizard-derived-state.ts` from 167 to 157 lines while preserving the existing wizard derived-state contract.
- Split Run Wizard selection and preflight assembly into `run-wizard-preflight-helpers.ts`, reducing `run-wizard-derived-helpers.ts` from 168 to 93 lines while preserving the existing target-device, fleet-count, and preflight behavior.
- Extracted approval facts and metadata rendering into `approval-details-facts.tsx`, reducing `approval-details-sections.tsx` from 155 to 124 lines while preserving the existing approval modal summary, role-access notice, and reject/approve behavior.
- Collapsed Run Wizard target-device counters and lock-derived lists into `buildRunWizardTargetState()` inside `run-wizard-derived-helpers.ts`, reducing `use-run-wizard-derived-state.ts` from 159 to 146 lines while preserving the existing wizard derived-state contract and preflight behavior.
- Extracted Devices page card-model mapping, filter logic, summary cohorts, and dispatch-risk list derivation into `devices-page-helpers.ts`, reducing `DevicesPage.tsx` from 155 to 142 lines while preserving the existing sync flow, drawer selection, summary drilldown, and risk panel behavior.
- Extracted `RunWizard` modal frame into `RunWizardModalLayout.tsx` and submit wiring into `use-run-wizard-submit-action.ts`, reducing `RunWizard.tsx` from 154 to 149 lines while preserving the existing wizard step-content props, footer behavior, navigation, and run dispatch flow.
- Extracted Device Setup stat/check/recovery primitives into `device-setup-card-primitives.tsx`, reducing `device-setup-cards.tsx` from 154 to 84 lines while preserving the existing Device Setup panel imports through re-exports.
- Extracted Device Setup checklist tone and detail helpers into `device-setup-checklist-helpers.ts`, reducing `device-setup-checklist.ts` to 142 lines while preserving the existing verification checklist content and probe guidance.
- Extracted Run Monitor step detail rendering into `run-monitor-step-details.tsx`, reducing `run-monitor-device-sections.tsx` from 161 to 120 lines while preserving the existing expand/collapse flow, raw step payload views, error panel, and screenshot preview.
- Extracted Device Setup guide support panels and protocol support panels into `device-setup-static-tab-panels.tsx`, reducing `device-setup-static-tabs.tsx` from 158 to 111 lines while preserving the existing onboarding, AutoJS bootstrap, environment reference, websocket contract, runtime expectation, and local runtime content.
- Extracted Run Monitor device status aggregation into `run-monitor-device-status-helpers.ts`, reducing `use-run-monitor-data.ts` from 158 to 109 lines while preserving the existing run fetch, pending-approval polling, realtime subscription, and per-device status contract.
- Extracted audit log insight types plus highlight and related-link builders into `audit-log-insight-types.ts` and `audit-log-insight-sections.ts`, reducing `audit-log-insights.ts` from 170 to 59 lines while preserving `buildAuditLogInsight()`, `getAuditDomainLabel()`, and `AUDIT_DOMAIN_ORDER` for existing callers.
- Extracted macro authoring modal state, template application, mode switching, JSON parsing, and submit validation into `use-macro-definition-authoring-state.ts`, reducing `macro-definition-authoring-modal.tsx` from 166 to 87 lines while preserving the existing modal layout, starter-template picker, builder/JSON toggle, and `onSubmit` contract.
- Extracted device drawer facts and raw metadata panels into `device-drawer-detail-sections.tsx`, reducing `DeviceDrawer.tsx` from 154 to 103 lines while preserving the existing drawer shell, device identity summary, battery card, lock-state display, and metadata rendering.
- Extracted runtime endpoint inputs and live probe result rendering into `device-setup-verify-sections.tsx`, reducing `device-setup-verify-panels.tsx` from 152 to 128 lines while preserving the existing endpoint editing, checklist display, selected-device probe controls, and current-app/screenshot result cards.
- Extracted approval requested-action parsing and metadata selection into `approval-requested-action.ts`, reducing `approval-insight-helpers.ts` from 152 to 101 lines while preserving the existing approval status labels, step config mapping, requested-action summary, and resolved-outcome text.
- Extracted Run Wizard selection, target-device derivation, lock snapshot building, target-state aggregation, and preflight memo wiring into `use-run-wizard-target-derivations.ts`, reducing `use-run-wizard-derived-state.ts` from 148 to 104 lines while preserving the existing `useRunWizardDerivedState()` return contract for `RunWizard`.
- Extracted Device Setup probe, reconnect, and expired-lock cleanup async flows into `device-setup-action-helpers.ts`, reducing `use-device-setup-verification-actions.ts` from 144 to 104 lines while preserving the existing hook return contract, toast messaging, selected-device probe behavior, and post-action verification refresh.
- Extracted live demo macro lookup, workflow-run creation, audit logging, and dispatch request flow into `demo-live-run-request.ts`, reducing `demo-runtime.ts` from 143 to 84 lines while preserving the existing simulated demo timing, live demo launch behavior, and `runLiveDemo()` / `runSimulatedDemo()` contracts used by `DemoPage`.
- Extracted Demo page run/reset orchestration into `use-demo-run-controller.ts`, reducing `DemoPage.tsx` from 166 to 127 lines while preserving the existing simulated/live mode behavior, active run cancellation, step reset timing, and panel wiring.
- Split Device Setup verification snapshot fetching and probe-selection helpers into `device-setup-verification-snapshot.ts` and `device-setup-verification-probe-helpers.ts`, reducing `device-setup-verification-runtime.ts` from 143 to 21 lines while preserving the existing verification fetch, selected-device fallback, live probe dispatch, and lock cleanup exports.
- Extracted Device Setup runtime endpoint state, normalized URL derivation, gateway websocket URL construction, and AutoJS bootstrap generation into `use-device-setup-runtime-config.ts`, reducing `use-device-setup-verification.ts` from 143 to 126 lines while preserving the existing hook return contract, verification refresh flow, selected-device fallback, and action wiring.
- Extracted Device Setup runtime and probe checklist item builders into `device-setup-checklist-items.ts`, reducing `device-setup-checklist.ts` from 142 to 65 lines while preserving the existing checklist item titles, tones, persistence guidance, and probe-detail copy.
- Extracted run-artifact step evidence and unlinked artifact rendering into `run-artifacts-evidence-sections.tsx`, reducing `run-artifacts-sections.tsx` from 143 to 67 lines while preserving the existing `RunArtifactsPanel` section exports, audit-focus badge behavior, per-step artifact grouping display, and unlinked artifact messaging.
- Extracted Device Setup live-probe checklist item builders into `device-setup-probe-checklist-items.ts`, reducing `device-setup-checklist-items.ts` from 146 to 113 lines while preserving the existing `buildDeviceSetupProbeChecklistItems()` export surface, probe tone mapping, and current-app/screenshot guidance.
- Extracted Run Wizard review device chips and populated input-variable sections into `run-wizard-review-detail-sections.tsx`, reducing `run-wizard-review-sections.tsx` from 141 to 117 lines while preserving the existing review summary metrics, device health/lock badges, input-variable display, and preflight pass/fail panel behavior.
- Extracted Macro Builder step-card header controls and policy controls into `macro-builder-step-card-sections.tsx`, reducing `macro-builder-step-card.tsx` from 143 to 39 lines while preserving the existing step ID/type editing, move/remove actions, guided-params editor wiring, approval toggle, and timeout/retry policy updates.
- Extracted orchestrator test async/mock builders into `orchestrator-test-mock-builders.ts`, reducing `orchestrator-test-helpers.ts` from 141 to 104 lines while preserving the existing `deferred()` and `createWorkflowRunsSupabaseMock()` import surface through re-exports for `orchestrator.test.ts`.
- Extracted Device Setup tab-content rendering into `device-setup-page-tab-content.tsx`, reducing `DeviceSetupPage.tsx` from 141 to 123 lines while preserving the existing page shell, verification hook wiring, and verify/guide/protocol/troubleshoot tab behavior.
- Extracted Laixi command-backed step execution handlers into `step-execution-helpers.ts`, reducing `mapper.ts` from 141 to 80 lines while preserving `executeStepOnDevice()` as the stable dispatch entrypoint and leaving the response-mapper exports untouched.
- Extracted Laixi tap/swipe batch-command handlers into `step-execution-batch-helpers.ts`, reducing `step-execution-helpers.ts` from 160 to 117 lines while preserving the existing `executeTapStep()` and `executeSwipeStep()` export surface through re-exports for `mapper.ts`.
- Extracted Devices page header sync action and device-lock warning into `devices-page-shell-sections.tsx`, keeping `DevicesPage.tsx` as a thinner page shell while preserving the existing sync flow, toast messaging, summary drilldown, filtered grid, and drawer selection.
- Extracted Run Monitor loading/not-found shell states and progress aggregation into `run-monitor-page-shell.tsx` and `run-monitor-progress.ts`, keeping `RunMonitorPage.tsx` as a thinner page shell while preserving the existing polling, cancel flow, approval modal wiring, auto-refresh toggle, and per-device expansion behavior.
- Extracted runner failure-screenshot capture and lock-renewal helpers into `runner-failure-screenshot.ts` and `runner-lock-renewal.ts`, reducing `runner.ts` to 128 lines while preserving the existing `WorkflowRunner` class contract used by runtime code and runner tests.
- Hardened repo-local agent instructions by updating `AGENTS.md`, `CLAUDE.md`, `docs/common-issues.md`, `docs/code-standards.md`, `docs/specify-workflow.md`, and `.claude/rules/` with bug-fix pre-checks, common-issue logging format, Karpathy coding principles, and plan verification rules adapted from `nextlevelbuilder/goclaw`.
- Synced the hard-priority plan/docs to the current git baseline and marked Phase 04 plus parent plan complete.

## Unresolved Questions

- Next post-threshold Spec Kit feature/spec still needs selection.
- Manual run-detail smoke for artifact evidence display is pending a suitable authenticated artifact-bearing run.
